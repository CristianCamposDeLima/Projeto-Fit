import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    boxTitle: {
        alignContent:"center",
        alignItems: "center",
        padding: 10,
    },
    textTitle: {
        color: "#000000",
    fontSize: 25,
        fontWeight: "bold",
    }

});
export default styles